# Lightweight-Eink-Server

Generate screenshots of various webUI for use by Eink screen

Put the built apps in a folder under public.
A corresponding get api will be exposed to get the rendered image.
The API will be the same as the folder name.
